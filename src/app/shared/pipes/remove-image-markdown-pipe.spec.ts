import { RemoveImageMarkdownPipe } from './remove-image-markdown-pipe';

describe('RemoveImageMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveImageMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
